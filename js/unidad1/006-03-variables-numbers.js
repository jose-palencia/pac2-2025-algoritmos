let resultado;

// PI
resultado = Math.PI;
console.log(`PI: ${resultado}`);

// Redondear
resultado = Math.round(2.9);
resultado = Math.round(2.1);
resultado = Math.round(2.5);

// Redondear hacia abajo
resultado = Math.floor(64.9);
resultado = Math.floor(64.5);
resultado = Math.floor(64.1);

// Redondear hacia arriba
resultado = Math.ceil(64.9);
resultado = Math.ceil(64.5);
resultado = Math.ceil(64.1);

// Raíz cuadrada
resultado = Math.sqrt(9);

// Valor absoluto
resultado = Math.abs(-100);

// Elevar a la potencia
resultado = Math.pow(8, 3);

// Mínimo
resultado = Math.min(1002, 99, 110, 1000, 70, 200);


// Máximo
resultado = Math.max(1002, 99, 110, 1000, 70, 200);


// Aleatorio
resultado = Math.random();
resultado = Math.random() * 20;
resultado = Math.round(Math.random() * 20);


console.log(resultado);


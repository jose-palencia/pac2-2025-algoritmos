// Declaración de Función

sumar();
function sumar() {
    console.log(2+2);
}

// Expresión de Función
const sumar2 = function() {
    console.log(3 + 3);
}

sumar2();

// alert, prompt, parseInt


/**
 * FUNCIÓN
 * 1. Es un bloque de código independiente que realiza una tarea especifica.
 * 2. Se define fuera de cualquier objeto o clase.
 * 3, Se llama directamente por su nombre.
 * 
 * imprimir()
 * 
 * MÉTODO
 * 1. Es una función que esta asociada a un objeto o clase.
 * 2. Se define dentro de un objeto o como parte de una clase.
 * 3. Se invoca a través del objeto o instancia del objeto.
 * 
 * Factura.imprimir()
 */
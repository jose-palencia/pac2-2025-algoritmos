const mascota = 'firulais';
const mascota2 = 'RANGER';

console.log(mascota.toUpperCase());
console.log(mascota2.toLowerCase());

const resultado = mascota.charAt(0).toUpperCase() + mascota.slice(1);

console.log(resultado);

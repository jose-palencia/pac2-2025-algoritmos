const numbers = [100, 200, 300, 400, 500, [1000, 2000, 3000]];


//console.log(numbers[5][1]);


// cuanto es tamaño del arreglo
console.log(numbers.length);

for(let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
    
}